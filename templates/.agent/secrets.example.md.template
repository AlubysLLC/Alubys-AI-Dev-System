# Secrets Reference
## [PROJECT NAME]

> Track secret names and where they are configured. Never store real secret values here.

| Secret Name | Used For | Where To Set It |
|-------------|----------|-----------------|
| EXAMPLE_API_KEY | Example external API access | Local environment or secret manager |

Real values belong in environment variables, a password manager, or `.agent/secrets.local.md`.
`.agent/secrets.local.md` is ignored by git.
